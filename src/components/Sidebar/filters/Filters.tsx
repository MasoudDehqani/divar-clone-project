import React, { useContext } from 'react'
import { DivarContext } from "../../context/divarContext";

const Filters = () => {

  const { data, routes, city } = useContext(DivarContext);

  return (
    <div>
      
    </div>
  )
}

export default Filters
